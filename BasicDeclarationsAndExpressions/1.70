#include <stdio.h>

int main()
{
	char c = 'A';
	
	while ( ( c <= 'z') && (c >= 'A')  )
	{
		if ( ( c > 'Z' ) && (c < 'a' ))
		{
			c+=1;
			continue;
		}
		if (c== 'a')
			printf("\n");
		printf("[%d-%c] ", c,c);
		c+=1;
	}

	return 0;
}
