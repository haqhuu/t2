#include <stdio.h>

int main()
{
	double sum = 1, tmp1 = 1, tmp2 = 1;
	int n = 3;
	
	for(int i = 0; i < n; i++)
	{
		tmp1 += 2;
		tmp2 *= 2;
		sum += (double) tmp1/tmp2;
	}
	
	printf("Value of series: %.2lf", sum);
	
	return 0;
}
