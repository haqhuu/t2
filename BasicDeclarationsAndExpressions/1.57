#include <stdio.h>

int main()
{
	int n, tmp = 0;
	
	printf("The original number = "); 
	scanf("%d", &n);
	
	while (n != 0)
	{
		tmp = tmp * 10 + n % 10;
		n /= 10;
	}
	n = tmp;
	printf("The reverse of the said number = %d", n);
	
	return 0;
}
