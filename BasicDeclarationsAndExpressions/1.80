#include <stdio.h>

int main(void)
{	
	int n = 0;

	while (n < 1 || n > 10)
	{
		printf("Input the size of the square (1-10): ");
		scanf("%d", &n);
	}
	
	for(int i = 0; i < n; i++)
	{
		for(int j = 0; j < n; j++)
		{
			printf("# ");
		}
		printf("\n");
	}

	return 0;
}
