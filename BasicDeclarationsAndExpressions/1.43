#include "stdio.h"

int main()
{
	int p, q;
	
	printf("Input number of lines: "); scanf("%d", &p);
	printf("Number of characters in a line: "); scanf("%d", &q);
	
	for (int i = 0; i < p; i++)
	{
		for (int j = 1; j <= q; j++)
		{
			printf("%d ", q*i+j);
		}
		printf("\n");
	}
	
	return 0;
}
